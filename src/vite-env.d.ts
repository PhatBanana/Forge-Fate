/// <reference types="vite/client" />

/** §102: "§<newest HISTORY section> · <short commit>", stamped by
    vite.config.ts at build time; '' in a build with no git. */
declare const __APP_VERSION__: string;
